Any and all implementation details that may or may not have been contained herein have been obfuscated.

bus_loc*.tcl
	Purpose: Tcl script to be ran inside ICC shell. Generates bus/port location stats to be processes by the the portswap perl script. 


bus_timing.tcl
	Purpose: Tcl script to be ran inside ICC shell. Generates bus/port timing stats to be processes by the the portswap perl script. 


find_splittable.tcl
	Purpose: Find flops that would be good candidates for duplication to ease timing and congestion.


placebus.tcl
	Purpose: Used for testing a data bus placement overhall to help timing and congestion


proctest.tcl
	Purpose: Some tcl procs written to help logic placement between interacting buses that were timing critical.
